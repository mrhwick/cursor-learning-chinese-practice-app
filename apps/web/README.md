# Web (React + Vite)

## Setup (macOS)

From the repo root:

```bash
cd apps/web
npm install
```

## Run

```bash
./run_dev.sh
```

Open:
- `http://localhost:5173`

The dev server proxies `/api/*` to `http://localhost:8000`.

