## Checkpoint: Lesson 00 end

You should be able to:
- run `./scripts/dev.sh`
- open `http://localhost:5173`
- change one UI string and see it in the browser

If you’re stuck:
- confirm `apps/web` has `node_modules` (run `npm install`)
- confirm `apps/api/.venv` exists and deps installed

