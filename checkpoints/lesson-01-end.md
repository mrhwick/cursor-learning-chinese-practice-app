## Checkpoint: Lesson 01 end

Expected:
- UI shows backend health status
- Vite proxies `/api/*` to the FastAPI server

Key files:
- `apps/web/vite.config.ts` (proxy)
- `apps/web/src/ui/App.tsx` (fetch call)
- `apps/api/chinese_practice/main.py` (`GET /api/health`)

