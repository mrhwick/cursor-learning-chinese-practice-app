## Checkpoint: Lesson 03 end

Expected:
- You can point to multiple API routes and describe request/response JSON.
- You added a small new endpoint (example: `/api/version`) and displayed it in the UI.

Key idea:
- FastAPI `response_model=...` turns Python objects into validated JSON shapes.

