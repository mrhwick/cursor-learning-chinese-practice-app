## Checkpoint: Lesson 06 end

Expected:
- You can sign up, log out, and log in again.
- You can explain where “logged in” state lives (cookie + session).

Key idea:
- `credentials: "include"` is required for cookies to be sent with `fetch`.

